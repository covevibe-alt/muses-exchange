# Secrets & credentials — NEVER commit
.env
.env.*
!.env.example
*.key
*.pem
.spotify-token.json

# Internal / non-shipping material
_internal/
*.command

# Python
__pycache__/
*.pyc
*.pyo

# Auto-generated by the price fetcher — recomputed on every run. Tracked
# historically but ignoring future edits so it stops appearing in every
# `git status` and doesn't get stashed around when pulling.
app/listener-ratios.json

# Node
node_modules/

# OS & editor junk
.DS_Store
Thumbs.db
.fuse_hidden*
.~lock*
*.swp
.idea/
.vscode/

# Logs
*.log
